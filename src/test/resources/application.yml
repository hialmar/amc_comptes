# Proprietes de l'application
spring:
  application:
    name: amccomptes
  profiles:
    active: dev
  cloud:
    config:
      enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: test